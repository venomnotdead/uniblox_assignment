/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.dummyjson.com', 'assets.dummyjson.com', 'via.placeholder.com', 'github.com'], // Add any other domains you need
  },
};

export default nextConfig;
