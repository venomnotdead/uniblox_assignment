/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.dummyjson.com', 'assets.dummyjson.com'], // Add any other domains you need
      },
};

export default nextConfig;
